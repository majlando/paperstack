# @paperstack/desktop

> **Frozen (2026-06-19).** Paperstack is converging on the VS Code extension
> (`apps/vscode`) as its single form — see [docs/DIRECTION.md](../../docs/DIRECTION.md).
> This Tauri app stays shippable (v0.2.0) but gets no new feature work, and is
> slated for retirement once the extension reaches franchise-parity (live PDF
> preview + zero-setup Typst). New work goes to the extension.

The Tauri 2 + React desktop app. Everything you need to run, test, and build
it — including the sidecar setup and the smoke-test hooks — is documented in
the [repository README](../../README.md) and [CLAUDE.md](../../CLAUDE.md).
