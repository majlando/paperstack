{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Paperstack Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "${workspaceFolder}/../../fixtures/demo-report"
      ],
      "outFiles": ["${workspaceFolder}/dist/**/*.js"],
      "preLaunchTask": "build extension"
    }
  ]
}
