# Preface

This demo report exists so Paperstack's engine tests have a small, fully
synthetic project to build. It exercises every feature the SEA template must
support: code blocks, figures, diagrams, tables, and section roles.

<!-- This HTML comment must be excluded from the normalsider count. -->
